{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Serve executor",
  "description": "",
  "type": "object",
  "properties": {
    "daemonMode": {
      "type": "boolean",
      "description": "Have the local subgraph node run in daemonMode"
    },
    "composePath": {
      "type": "string",
      "description": "Path to docker-compose path"
    }
  },
  "required": ["daemonMode", "composePath"]
}

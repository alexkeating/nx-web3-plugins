{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Serve executor",
  "description": "",
  "type": "object",
  "properties": {
    "daemonMode": {
      "type": "string",
      "description": "Have the local subgraph node run in daemonMode"
    }
  },
  "required": ["daemonMode"]
}
